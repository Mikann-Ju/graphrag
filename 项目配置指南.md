# GraphRAG 项目配置指南

## 环境要求

### 系统要求
- Python 3.10 - 3.12
- 至少 8GB RAM（推荐 16GB+）
- 足够的磁盘空间用于存储索引数据

### 必需的服务
- OpenAI API 密钥（或其他支持的LLM服务）
- 可选：Azure 服务（用于存储和搜索）

## 安装步骤

### 1. 克隆项目
```bash
git clone https://github.com/microsoft/graphrag.git
cd graphrag
```

### 2. 安装 Python 依赖

#### 方法一：使用 Poetry（推荐）
```bash
# 安装 Poetry（如果还没有）
curl -sSL https://install.python-poetry.org | python3 -

# 安装依赖
poetry install

# 激活虚拟环境
poetry shell
```

#### 方法二：使用 pip
```bash
# 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Linux/Mac
# 或
venv\Scripts\activate  # Windows

# 安装 GraphRAG
pip install graphrag
```

### 3. 安装统一搜索应用
```bash
cd unified-search-app
poetry install
```

## 配置设置

### 1. 环境变量配置

创建 `.env` 文件：
```bash
# OpenAI 配置
OPENAI_API_KEY=your_openai_api_key_here

# Azure 配置（可选）
AZURE_OPENAI_API_KEY=your_azure_openai_key
AZURE_OPENAI_ENDPOINT=your_azure_openai_endpoint
AZURE_SEARCH_ENDPOINT=your_azure_search_endpoint
AZURE_SEARCH_KEY=your_azure_search_key
AZURE_STORAGE_CONNECTION_STRING=your_storage_connection_string
AZURE_COSMOSDB_CONNECTION_STRING=your_cosmosdb_connection_string

# 其他配置
GRAPH_CACHE_TYPE=memory  # memory, file, cosmosdb
```

### 2. 项目初始化

```bash
# 创建新项目
graphrag init --root /path/to/your/project

# 这会创建以下目录结构：
# your_project/
# ├── input/           # 输入文档
# ├── output/          # 索引输出
# ├── settings.yml     # 配置文件
# └── prompts/         # 提示模板
```

### 3. 配置文件设置

编辑 `settings.yml`：

```yaml
# 语言模型配置
language_models:
  openai:
    type: "openai"
    api_key: "${OPENAI_API_KEY}"
    model: "gpt-4"
    temperature: 0.0
    max_tokens: 4000
    concurrent_requests: 4
    async_mode: "asyncio"

# 文档分块配置
chunks:
  strategy: "fixed_size"  # fixed_size, semantic
  size: 1000              # 每个块的最大token数
  overlap: 200            # 块之间的重叠token数
  encoding_model: "cl100k_base"
  group_by_columns: []    # 按列分组文档
  prepend_metadata: false
  chunk_size_includes_metadata: false

# 图提取配置
extract_graph:
  model_id: "openai"
  entity_types: ["person", "organization", "location", "concept", "event", "product"]
  strategy:
    type: "default"
    prompt_file: "prompts/extract_graph.txt"

# 描述总结配置
summarize_descriptions:
  model_id: "openai"
  strategy:
    type: "default"
    prompt_file: "prompts/summarize_descriptions.txt"

# 社区检测配置
cluster_graph:
  max_cluster_size: 50
  use_lcc: true          # 使用最大连通分量
  seed: 42

# 社区报告配置
create_community_reports:
  model_id: "openai"
  strategy:
    type: "default"
    prompt_file: "prompts/create_community_reports.txt"

# 嵌入生成配置
generate_text_embeddings:
  model_id: "openai"
  batch_size: 100

# 本地搜索配置
local_search:
  chat_model_id: "openai"
  embedding_model_id: "openai"
  text_unit_prop: 0.7    # 文本单元比例
  community_prop: 0.3    # 社区信息比例
  top_k_entities: 10
  top_k_relationships: 10
  conversation_history_max_turns: 10
  conversation_history_user_turns_only: true

# 全局搜索配置
global_search:
  chat_model_id: "openai"
  map_system_prompt_file: "prompts/global_search_map.txt"
  reduce_system_prompt_file: "prompts/global_search_reduce.txt"
  dynamic_community_selection: false

# DRIFT搜索配置
drift_search:
  chat_model_id: "openai"
  local_system_prompt_file: "prompts/drift_search_local.txt"
  reduce_system_prompt_file: "prompts/drift_search_reduce.txt"

# 输出配置
output:
  base_dir: "output"
  format: "parquet"

# 缓存配置
cache:
  type: "memory"  # memory, file, cosmosdb
  file:
    base_dir: "cache"
  cosmosdb:
    connection_string: "${AZURE_COSMOSDB_CONNECTION_STRING}"
    database_name: "graphrag_cache"
    container_name: "llm_cache"

# 快照配置
snapshots:
  raw_graph: false
  intermediate_steps: false
```

## 准备数据

### 1. 文档格式

支持以下格式：
- **CSV**: 包含 `id`, `text`, `metadata` 列
- **JSON**: 包含 `id`, `text`, `metadata` 字段
- **Markdown**: 自动提取文本内容
- **TXT**: 纯文本文件

### 2. 数据放置

将文档放在 `input/` 目录中：
```
input/
├── documents.csv
├── articles/
│   ├── article1.md
│   └── article2.txt
└── data/
    └── dataset.json
```

### 3. 示例数据

创建 `input/documents.csv`：
```csv
id,text,metadata
doc1,"GraphRAG是一个基于知识图谱的检索增强生成系统。它通过构建结构化的知识图谱来增强大语言模型对私有数据的推理能力。",{"source":"research_paper","date":"2024-01-01"}
doc2,"知识图谱是一种结构化的知识表示方法，它将实体和关系组织成图结构。",{"source":"tutorial","date":"2024-01-02"}
```

## 运行索引

### 1. 基本索引
```bash
# 运行完整索引
graphrag index --root /path/to/your/project

# 使用快速模式（减少LLM调用）
graphrag index --root /path/to/your/project --method fast

# 启用缓存
graphrag index --root /path/to/your/project --cache

# 详细输出
graphrag index --root /path/to/your/project --verbose
```

### 2. 监控进度

索引过程会显示进度信息：
```
Workflow started: load_input_documents
Workflow completed: load_input_documents
Workflow started: create_base_text_units
Progress: 25% - Processing documents...
Workflow completed: create_base_text_units
...
```

### 3. 输出文件

索引完成后，`output/` 目录将包含：
```
output/
├── entities.parquet          # 实体数据
├── relationships.parquet     # 关系数据
├── text_units.parquet        # 文本单元
├── communities.parquet       # 社区数据
├── community_reports.parquet # 社区报告
└── embeddings/               # 嵌入向量
    ├── text_units/
    └── entities/
```

## 运行查询

### 1. 命令行查询
```bash
# 基本查询
graphrag query --root /path/to/your/project "什么是GraphRAG？"

# 指定搜索模式
graphrag query --root /path/to/your/project --search-type local "GraphRAG的主要功能是什么？"
graphrag query --root /path/to/your/project --search-type global "整个数据集的主要主题是什么？"
graphrag query --root /path/to/your/project --search-type drift "详细解释知识图谱的作用"
```

### 2. Python API
```python
from graphrag import GraphRAG

# 初始化
rag = GraphRAG.from_project_root("/path/to/your/project")

# 查询
response = rag.query("什么是GraphRAG？")
print(response)

# 指定搜索模式
response = rag.query("GraphRAG的主要功能是什么？", search_type="local")
print(response)
```

### 3. 统一搜索应用

```bash
# 启动Web应用
cd unified-search-app
poetry run streamlit run app/home_page.py

# 或使用生产模式
poetry run streamlit run app/home_page.py --server.port=8501 --server.address=0.0.0.0
```

访问 http://localhost:8501 使用Web界面。

## 高级配置

### 1. 自定义提示

编辑 `prompts/` 目录中的提示文件：

```bash
# 图提取提示
prompts/extract_graph.txt:
从以下文本中提取实体和关系。识别人物、组织、地点、概念等实体，以及它们之间的关系。

文本：{text}

请以JSON格式返回：
{
  "entities": [
    {"title": "实体名称", "type": "实体类型", "description": "实体描述"}
  ],
  "relationships": [
    {"source": "源实体", "target": "目标实体", "description": "关系描述"}
  ]
}
```

### 2. 自定义工作流程

```python
from graphrag.index.workflows.factory import PipelineFactory
from graphrag.index.typing.workflow import WorkflowFunctionOutput

def custom_workflow(config, context):
    # 自定义处理逻辑
    return WorkflowFunctionOutput(result=output)

# 注册自定义工作流程
PipelineFactory.register("custom_workflow", custom_workflow)
```

### 3. 性能优化

```yaml
# 增加并发请求数
language_models:
  openai:
    concurrent_requests: 8  # 根据API限制调整

# 调整批处理大小
generate_text_embeddings:
  batch_size: 200

# 使用更快的模型
language_models:
  openai:
    model: "gpt-3.5-turbo"  # 更快的模型
```

## 故障排除

### 1. 常见错误

**API密钥错误**：
```
Error: Invalid API key
```
解决：检查环境变量中的API密钥是否正确设置。

**内存不足**：
```
MemoryError: Unable to allocate array
```
解决：减少批处理大小或使用更小的文档块。

**网络超时**：
```
TimeoutError: Request timed out
```
解决：增加超时时间或检查网络连接。

### 2. 调试模式

```bash
# 启用详细日志
export GRAPHRAG_LOG_LEVEL=DEBUG
graphrag index --root /path/to/your/project --verbose

# 检查配置
graphrag init --root /path/to/your/project --force
```

### 3. 性能监控

```bash
# 内存分析
graphrag index --root /path/to/your/project --memprofile

# 缓存状态
ls -la cache/  # 检查缓存文件
```

## 最佳实践

### 1. 数据准备
- 确保文档质量高，内容相关
- 合理设置文档块大小（1000-2000 tokens）
- 使用有意义的文档ID和元数据

### 2. 配置优化
- 根据数据量调整并发请求数
- 使用缓存减少重复的LLM调用
- 定期清理缓存文件

### 3. 监控和维护
- 监控API使用量和成本
- 定期更新索引数据
- 备份重要的配置和提示文件

### 4. 扩展性
- 使用Azure存储进行大规模部署
- 实现自定义工作流程
- 集成到现有系统中

## 下一步

1. **阅读文档**: 查看 [官方文档](https://microsoft.github.io/graphrag)
2. **尝试示例**: 运行 `examples_notebooks/` 中的示例
3. **参与社区**: 在 [GitHub Discussions](https://github.com/microsoft/graphrag/discussions) 中提问
4. **贡献代码**: 查看 [CONTRIBUTING.md](./CONTRIBUTING.md)

现在您已经完成了GraphRAG的基本配置！可以开始构建您的知识图谱并享受强大的检索增强生成功能。 