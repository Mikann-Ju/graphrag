# GraphRAG 游戏智能分析项目配置指南

## 项目概述

本项目将GraphRAG技术应用于休闲游戏（消消乐、纸牌、桌球等）的付费场次分析，通过构建玩家-场次-付费行为知识图谱，实现智能游戏运营分析。

## 🆕 Deep Search 新功能

本项目现已集成了最新的 **DeepSearch** 功能，专为游戏数据分析优化：

### 游戏分析专用功能
- **🎯 多维度分析路径**: 可以同时分析玩家行为、付费模式、游戏进度等多个维度
- **🔍 付费行为逻辑链**: 可视化玩家从免费到付费的完整决策链路
- **🚀 智能运营建议**: 结合多种搜索策略生成个性化运营策略
- **📊 置信度评估**: 评估分析结果的可靠性，支持数据驱动决策
- **⚡ 自适应深度**: 根据数据复杂度自动调整分析深度

### 游戏场景应用示例

#### 付费转化分析
```bash
# 分析付费转化路径
graphrag query --method deep --query "高价值玩家的付费转化路径是什么？"

# 分析流失原因
graphrag query --method deep --query "为什么玩家在第3关后容易流失？"
```

#### 游戏平衡性分析
```bash
# 关卡难度分析
graphrag query --method deep --query "哪些关卡的难度设置可能影响玩家体验？"

# 道具使用模式
graphrag query --method deep --query "玩家如何使用游戏道具来通过困难关卡？"
```

#### 运营策略优化
```bash
# 活动效果评估
graphrag query --method deep --query "促销活动对不同类型玩家的影响如何？"

# 个性化推荐
graphrag query --method deep --query "如何为不同玩家群体设计个性化的游戏体验？"
```

### 可视化分析界面
启用 Deep Search 后，您可以在 Web 界面中看到：
- **玩家行为路径图**: 可视化玩家的游戏和付费行为轨迹
- **置信度分析**: 评估分析结果的可靠性
- **多维度关联图**: 展示游戏要素之间的复杂关系

## 环境要求

### 系统要求
- Python 3.10 - 3.12
- 至少 16GB RAM（推荐 32GB+，用于处理大量游戏数据）
- 至少 100GB 磁盘空间（用于存储游戏数据和索引）

### 必需的服务
- OpenAI API 密钥（或其他支持的LLM服务）
- AWS S3 存储（可选，用于生产环境）
- Apache Flink（可选，用于实时数据处理）

## 安装步骤

### 1. 克隆项目
```bash
git clone https://github.com/microsoft/graphrag.git
cd graphrag
```

### 2. 安装 Python 依赖

#### 方法一：使用 Poetry（推荐）
```bash
# 安装 Poetry（如果还没有）
curl -sSL https://install.python-poetry.org | python3 -

# 安装依赖
poetry install

# 安装游戏数据分析专用包
poetry add pandas numpy boto3 streamlit plotly
yy
# 激活虚拟环境
poetry shell
```

#### 方法二：使用 pip
```bash
# 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Linux/Mac
# 或
venv\Scripts\activate  # Windows

# 安装 GraphRAG
pip install graphrag

# 安装游戏数据分析专用包
pip install pandas numpy boto3 streamlit plotly
```

### 3. 安装统一搜索应用
```bash
cd unified-search-app
poetry install
```

### 4. 生成游戏模拟数据
```bash
# 运行数据生成器
python game_data_generator.py

# 这将在 game_data/ 目录中生成：
# ├── raw/                 # 原始数据
# │   ├── players.csv      # 玩家数据
# │   ├── rooms.csv        # 场次数据  
# │   ├── game_sessions.csv # 对局记录数据
# │   └── payment_records.csv # 付费记录
# ├── input/               # GraphRAG输入
# │   └── documents.csv    # 文本文档
# └── data_stats.json      # 数据统计
```

## 配置设置

### 1. 环境变量配置

创建 `.env` 文件：
```bash
# OpenAI 配置
OPENAI_API_KEY=your_openai_api_key_here

# 游戏数据配置
GAME_DATA_BUCKET=game-data-lake
FLINK_CLUSTER_URL=localhost:8081

# AWS S3 配置（生产环境）
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_DEFAULT_REGION=us-west-2

# Redis缓存配置（可选）
REDIS_URL=redis://localhost:6379

# 其他配置
GRAPH_CACHE_TYPE=memory  # memory, file, redis
LOG_LEVEL=INFO
```

### 2. 项目初始化

```bash
# 创建游戏分析项目
graphrag init --root /path/to/your/game_project

# 复制生成的游戏数据
cp game_data/input/documents.csv /path/to/your/game_project/input/

# 这会创建以下目录结构：
# game_project/
# ├── input/               # 输入文档（游戏数据）
# ├── output/              # 索引输出
# ├── settings.yml         # 配置文件
# ├── prompts/             # 提示模板
# └── .env                 # 环境变量
```

### 3. 游戏数据配置文件

编辑 `settings.yml` - 游戏专用配置：

```yaml
# 语言模型配置 - 针对游戏分析优化
language_models:
  # OpenAI 配置示例
  openai:
    type: "openai"
    api_key: "${OPENAI_API_KEY}"
    model: "gpt-4"           # 使用GPT-4获得更好的游戏分析效果
    temperature: 0.1         # 低温度确保分析的一致性
    max_tokens: 4000
    concurrent_requests: 8   # 游戏数据量大，增加并发
    async_mode: "asyncio"    # 开启异步处理提升速度
  
  # Gemini 2.5 Pro 配置示例（使用OpenAI兼容接口）
  gemini:
    type: "openai"           # 使用OpenAI兼容类型（GraphRAG暂不直接支持Google）
    api_key: "${GEMINI_API_KEY}"
    model: "gemini-2.5-pro"  # Gemini 2.5 Pro模型
    encoding_model: "cl100k_base"  # 继续使用OpenAI编码器（兼容性最好）
    temperature: 0.1         # 低温度确保游戏分析一致性
    max_tokens: 8192         # Gemini支持更长的上下文
    concurrent_requests: 6   # 根据Gemini API限制调整
    async_mode: "asyncio"    # 开启异步处理
    # 注意：可能需要配置API网关或代理来兼容OpenAI格式

# 文档分块配置 - 适配游戏文档结构
chunks:
  # 切分策略选择
  strategy: "tokens"        # 推荐：基于token的精确切分
  # strategy: "sentence"    # 备选：基于句子的语义切分（适合长文档）
  # strategy: "fixed_size"  # 别名：等同于tokens
  
  size: 800                 # 游戏数据文档相对较短，800 tokens合适
  overlap: 150              # 重叠150 tokens，保持玩家行为上下文连续性
  encoding_model: "cl100k_base"  # GPT-4编码器，1 token ≈ 0.75个英文单词
  
  # 分组配置 - 按文档类型分别处理
  group_by_columns: ["metadata.document_type"]  # 按文档类型分组
  # 可选的其他分组方式：
  # group_by_columns: ["metadata.game_type"]     # 按游戏类型分组
  # group_by_columns: ["metadata.player_segment"] # 按玩家分群分组
  
  prepend_metadata: true    # 在每个块前添加元数据标签
  chunk_size_includes_metadata: false  # 元数据不计入chunk size

# 图提取配置 - 游戏实体类型
extract_graph:
  model_id: "openai"      # 或 "gemini" 如果使用Gemini
  entity_types: [
    "PLAYER",               # 玩家 - 游戏用户及其属性
    "ROOM",                 # 场次 - 游戏房间和场次配置  
    "PAYMENT",              # 付费行为 - 充值、购买等付费数据
    "GAME_RECORD",          # 对局记录 - 具体的游戏局次和战绩数据
    "PLAYER_SEGMENT",       # 玩家分群 - 玩家类型和群体分类
    "GAME_TYPE",           # 游戏类型 - 消消乐、纸牌、桌球等
    "INVESTMENT_STYLE",     # 投资风格 - 玩家的投资和消费偏好
    "RISK_FACTOR",         # 风险因子 - 影响游戏体验的风险要素
    "OPTIMIZATION_STRATEGY" # 优化策略 - 运营优化和改进方案
  ]
  strategy:
    type: "graph_intelligence"  # 使用AI智能提取策略
    # 自定义参数（可选）
    extraction_prompt: "prompts/extract_game_entities.txt"  # 自定义提示文件路径
    max_gleanings: 2           # 最多提取轮次（提高准确率）
    tuple_delimiter: "<|>"     # 元组分隔符
    record_delimiter: "##"     # 记录分隔符  
    completion_delimiter: "<|COMPLETE|>"  # 完成标记

# 游戏实体总结配置
summarize_descriptions:
  model_id: "openai"
  strategy:
    type: "game_focused"
    prompt_file: "prompts/summarize_game_entities.txt"

# 社区检测配置 - 发现玩家群体
cluster_graph:
  max_cluster_size: 30      # 较小的社区便于分析玩家群体
  use_lcc: true
  seed: 42

# 社区报告配置 - 生成玩家群体分析
create_community_reports:
  model_id: "openai"
  strategy:
    type: "player_community_analysis"  # 玩家社区分析类型
    prompt_file: "prompts/create_player_community_reports.txt"
    max_tokens: 1500                   # 报告最大长度
    max_input_tokens: 8000            # 输入上下文最大长度

# 嵌入生成配置
generate_text_embeddings:
  model_id: "openai"              # 嵌入模型
  batch_size: 50                  # 批处理大小
  embedding_dimension: 1536       # 向量维度（OpenAI text-embedding-3-small）
  skip_existing: true             # 跳过已存在的嵌入

# 🏠 本地搜索配置 - 精准玩家/场次查询
local_search:
  chat_model_id: "openai"              # 使用的AI模型
  embedding_model_id: "openai"         # 嵌入模型
  text_unit_prop: 0.6                  # 文本内容权重60% - 优先展示具体游戏行为
  community_prop: 0.4                  # 社区信息权重40% - 结合玩家群体背景
  top_k_entities: 15                   # 返回15个最相关实体
  top_k_relationships: 15              # 返回15个最相关关系
  conversation_history_max_turns: 10   # 最多记住10轮对话历史
  max_context_tokens: 8000            # 上下文最大token数

# 🌍 全局搜索配置 - 整体趋势分析
global_search:
  chat_model_id: "openai"
  map_system_prompt_file: "prompts/game_global_search_map.txt"     # Map阶段提示
  reduce_system_prompt_file: "prompts/game_global_search_reduce.txt" # Reduce阶段提示
  dynamic_community_selection: true   # 动态选择相关玩家群体
  max_context_tokens: 12000           # 更大的上下文窗口支持全局分析
  concurrency: 32                     # 并发处理社区报告数量

# 🌊 DRIFT搜索配置 - 平衡深度分析
drift_search:
  chat_model_id: "openai"
  embedding_model_id: "openai"
  local_system_prompt_file: "prompts/game_drift_search_local.txt"   # 本地分析提示
  reduce_system_prompt_file: "prompts/game_drift_search_reduce.txt" # 汇总分析提示
  # 本地搜索参数
  local_search_text_unit_prop: 0.7     # 本地阶段文本权重
  local_search_community_prop: 0.3     # 本地阶段社区权重
  local_search_top_k_mapped_entities: 20  # 本地阶段实体数
  # 全局参数
  max_context_tokens: 16000            # 支持更大的分析范围
  reduction_max_tokens: 2000           # 汇总结果最大长度

# 📤 输出配置
output:
  type: "file"              # 输出类型: file, blob, cosmosdb
  base_dir: "output"        # 输出目录
  format: "parquet"         # 文件格式: parquet高效，csv可读性好
  # storage:                # 云存储配置（可选）
  #   connection_string: "${STORAGE_CONNECTION_STRING}"
  #   container_name: "game-analysis-results"

# 📥 输入配置  
input:
  storage:
    type: "file"            # 输入类型: file, blob
    base_dir: "input"       # 输入目录
  file_type: "csv"          # 文件类型: csv, text, json
  encoding: "utf-8"         # 文件编码

# 🗄️ 缓存配置 - 游戏数据分析缓存策略
cache:
  type: "file"              # 缓存类型: file(持久), memory(快速), redis(分布式)
  file:
    base_dir: "cache"       # 缓存目录
    max_size: "10GB"        # 游戏数据缓存限制
    ttl: 3600              # 缓存生存时间(秒)

# 📸 快照配置
snapshots:
  raw_graph: true          # 保存原始知识图谱用于调试
  intermediate_steps: true # 保存中间处理步骤用于优化
  entities: true           # 保存实体快照
  relationships: true      # 保存关系快照
  communities: true        # 保存社区快照
  strategy:
    schedule: "daily"      # 快照频率: daily, weekly, manual
    retention: 30          # 保留天数
    compression: true      # 启用压缩节省空间
```

## 游戏数据特定提示模板

### 1. 游戏实体提取提示

创建 `prompts/extract_game_entities.txt`：
```
-Goal-
从游戏运营文档中提取实体和关系，专注于游戏业务场景的实体识别和关系建模。

-Steps-
1. 识别游戏相关实体。对每个实体提取：
- entity_name: 实体名称，首字母大写
- entity_type: 实体类型，必须是以下之一: [{entity_types}]
- entity_description: 详细描述实体的游戏属性、行为模式和业务价值

格式：("entity"{tuple_delimiter}<entity_name>{tuple_delimiter}<entity_type>{tuple_delimiter}<entity_description>)

2. 识别实体间的游戏业务关系。对每个关系提取：
- source_entity: 源实体名称
- target_entity: 目标实体名称  
- relationship_description: 关系的游戏业务含义和影响
- relationship_strength: 关系强度(1-10，10最强)

格式：("relationship"{tuple_delimiter}<source_entity>{tuple_delimiter}<target_entity>{tuple_delimiter}<relationship_description>{tuple_delimiter}<relationship_strength>)

3. 返回所有识别的实体和关系，使用 **{record_delimiter}** 分隔

4. 完成后输出 {completion_delimiter}

######################
-Examples-
######################
实体类型示例: PLAYER,ROOM,PAYMENT,GAME_RECORD,PLAYER_SEGMENT
文本:
张三是一位VIP3级玩家，在2024年1月15日进入消消乐房间A001，门槛费50金币。他在这局游戏中获得8900分，随后充值100元购买道具包。张三属于高价值玩家群体，偏好中高难度场次。
######################
输出:
("entity"{tuple_delimiter}张三{tuple_delimiter}PLAYER{tuple_delimiter}VIP3级玩家，偏好中高难度场次，属于高价值用户群体)
{record_delimiter}
("entity"{tuple_delimiter}房间A001{tuple_delimiter}ROOM{tuple_delimiter}消消乐游戏房间，门槛费50金币，中等难度设置)
{record_delimiter}
("entity"{tuple_delimiter}充值100元{tuple_delimiter}PAYMENT{tuple_delimiter}玩家付费行为，购买道具包提升游戏体验)
{record_delimiter}
("entity"{tuple_delimiter}高价值玩家群体{tuple_delimiter}PLAYER_SEGMENT{tuple_delimiter}付费意愿强、游戏活跃度高的核心用户群体)
{record_delimiter}
("relationship"{tuple_delimiter}张三{tuple_delimiter}房间A001{tuple_delimiter}张三参与房间A001的消消乐游戏，支付门槛费并获得高分{tuple_delimiter}8)
{record_delimiter}
("relationship"{tuple_delimiter}张三{tuple_delimiter}充值100元{tuple_delimiter}张三在游戏后进行付费购买道具包{tuple_delimiter}9)
{record_delimiter}
("relationship"{tuple_delimiter}张三{tuple_delimiter}高价值玩家群体{tuple_delimiter}张三属于高价值玩家群体，具备典型的高付费和高活跃特征{tuple_delimiter}10)
{completion_delimiter}

######################
-Real Data-
######################
实体类型: {entity_types}
文档内容: {input_text}
######################
输出:
```

### 2. 玩家社区分析提示

创建 `prompts/create_player_community_reports.txt`：
```
基于以下玩家群体信息，生成详细的社区分析报告：

分析维度：
1. 群体特征描述
2. 付费行为模式
3. 游戏偏好分析
4. 风险评估
5. 运营建议

重点关注：
- 群体的共同特征
- 付费能力和意愿
- 场次偏好和行为模式
- 流失风险和留存策略
- 个性化推荐策略

社区数据：{context}

请生成专业的游戏运营分析报告。
```

### 3. 游戏全局搜索提示

创建 `prompts/game_global_search_map.txt`：
```
你是一个专业的游戏数据分析师。基于以下游戏运营数据，回答用户的问题。

分析重点：
1. 从业务角度解读数据
2. 识别关键趋势和模式
3. 评估风险和机会
4. 提供可操作的建议

数据上下文：{context}
用户问题：{query}

请提供专业的分析，包含：
- 数据解读
- 关键发现
- 业务影响
- 行动建议
```

## 运行游戏数据索引

### 1. 基本索引
```bash
# 运行完整游戏数据索引
graphrag index --root /path/to/your/game_project

# 使用详细输出查看进度
graphrag index --root /path/to/your/game_project --verbose

# 启用缓存加速重建
graphrag index --root /path/to/your/game_project --cache
```

### 2. 索引进度监控

游戏数据索引过程：
```
Workflow started: load_game_input_data
Progress: 10% - 加载游戏文档...
Workflow completed: load_game_input_data

Workflow started: extract_game_entities_relationships  
Progress: 30% - 提取游戏实体和关系...
Progress: 50% - 处理玩家数据...
Progress: 70% - 处理场次数据...
Workflow completed: extract_game_entities_relationships

Workflow started: discover_player_communities
Progress: 85% - 发现玩家群体...
Workflow completed: discover_player_communities

索引完成！生成了 {entity_count} 个实体和 {relationship_count} 个关系
```

### 3. 输出文件结构

索引完成后，`output/` 目录将包含：
```
output/
├── entities.parquet          # 游戏实体（玩家、场次、付费等）
├── relationships.parquet     # 游戏关系（参与、付费、偏好等）
├── text_units.parquet        # 文本单元
├── communities.parquet       # 玩家社区数据
├── community_reports.parquet # 玩家群体分析报告
├── embeddings/               # 嵌入向量
│   ├── text_units/
│   └── entities/
└── game_analysis/            # 游戏专用分析结果
    ├── player_profiles/
    ├── room_analytics/
    └── payment_insights/
```

## 游戏数据查询示例

### 1. 命令行查询
```bash
# 玩家流失风险分析
graphrag query --root /path/to/your/game_project --search-type local "哪些玩家有高流失风险？他们的特征是什么？"

# 场次收益优化
graphrag query --root /path/to/your/game_project --search-type global "如何优化付费场次的门槛费设置来提升收益？"

# 深度分析
graphrag query --root /path/to/your/game_project --search-type drift "分析VIP玩家的付费模式，并提供个性化运营策略"

# 实时监控查询
graphrag query --root /path/to/your/game_project "最近一周有哪些异常的付费行为模式？"
```

### 2. Python API 游戏分析
```python
from graphrag import GraphRAG
import pandas as pd

# 初始化游戏分析引擎
game_rag = GraphRAG.from_project_root("/path/to/your/game_project")

# 玩家分析
def analyze_player_churn_risk():
    """分析玩家流失风险"""
    response = game_rag.query(
        "识别流失风险高的玩家群体，分析其特征并提供挽回策略",
        search_type="local"
    )
    return response

# 场次优化分析
def optimize_room_configuration():
    """优化场次配置"""
    response = game_rag.query(
        "基于玩家满意度和收益数据，分析各类场次的优化空间",
        search_type="global"
    )
    return response

# 付费行为分析
def analyze_payment_patterns():
    """分析付费模式"""
    response = game_rag.query(
        "分析不同玩家群体的付费模式，识别高价值用户特征",
        search_type="drift"
    )
    return response

# 运行分析
churn_analysis = analyze_player_churn_risk()
room_optimization = optimize_room_configuration()
payment_analysis = analyze_payment_patterns()

print("=== 流失风险分析 ===")
print(churn_analysis)
print("\n=== 场次优化建议 ===")
print(room_optimization)
print("\n=== 付费模式分析 ===")
print(payment_analysis)
```

### 3. 游戏运营 Dashboard

启动游戏分析Web应用：
```bash
# 启动增强版统一搜索应用
cd unified-search-app
export GAME_DATA_MODE=true
poetry run streamlit run app/game_analytics_page.py

# 访问 http://localhost:8501 使用游戏分析界面
```

Dashboard 功能：
- **实时KPI监控**：玩家活跃度、付费转化率、流失率
- **玩家分群分析**：不同玩家群体的行为模式
- **场次收益分析**：各场次的参与度和盈利能力
- **智能问答**：自然语言查询游戏数据
- **预警系统**：异常检测和风险提醒

## 高级功能配置

### 1. S3 数据湖集成

配置S3存储：
```yaml
# 在 settings.yml 中添加
storage:
  type: "s3"
  s3:
    bucket: "${GAME_DATA_BUCKET}"
    prefix: "game_analytics/"
    region: "${AWS_DEFAULT_REGION}"
    
data_lake:
  raw_data_path: "s3://game-data-lake/raw/"
  processed_data_path: "s3://game-data-lake/processed/" 
  features_path: "s3://game-data-lake/features/"
```

### 2. Flink 实时处理

集成Flink流处理：
```python
# flink_integration.py
from pyflink.datastream import StreamExecutionEnvironment
from game_analytics.processors import GameEventProcessor

def setup_flink_pipeline():
    env = StreamExecutionEnvironment.get_execution_environment()
    
    # 配置检查点
    env.enable_checkpointing(30000)
    
    # 添加游戏事件源
    game_events = env.add_source(GameEventSource())
    
    # 实时分析
    analysis_results = game_events.map(GameEventProcessor())
    
    # 输出到GraphRAG
    analysis_results.add_sink(GraphRAGSink())
    
    env.execute("GameDataPipeline")

if __name__ == "__main__":
    setup_flink_pipeline()
```

### 3. 自定义游戏分析工作流

```python
# custom_game_workflows.py
from graphrag.index.workflows.factory import PipelineFactory

def calculate_player_value_metrics(config, context):
    """计算玩家价值指标"""
    # 自定义玩家价值计算逻辑
    pass

def detect_payment_anomalies(config, context):
    """检测付费异常"""
    # 自定义异常检测逻辑
    pass

def generate_personalized_recommendations(config, context):
    """生成个性化推荐"""
    # 自定义推荐逻辑
    pass

# 注册自定义工作流程
PipelineFactory.register("calculate_player_value", calculate_player_value_metrics)
PipelineFactory.register("detect_anomalies", detect_payment_anomalies)
PipelineFactory.register("generate_recommendations", generate_personalized_recommendations)
```

## 性能优化和监控

### 1. 游戏数据处理优化

```yaml
# 优化配置
performance:
  # 增加并发处理
  concurrent_requests: 16
  batch_size: 200
  
  # 内存优化
  chunk_size: 500
  max_memory_usage: "8GB"
  
  # 缓存策略
  cache_strategy: "aggressive"
  cache_ttl: 3600
```

### 2. 监控指标

```python
# monitoring.py
class GameAnalyticsMonitor:
    def __init__(self):
        self.metrics = {
            "query_latency": [],
            "index_build_time": 0,
            "data_freshness": 0,
            "accuracy_score": 0
        }
    
    def track_query_performance(self, query, response_time):
        """跟踪查询性能"""
        self.metrics["query_latency"].append(response_time)
    
    def monitor_data_quality(self, entities, relationships):
        """监控数据质量"""
        quality_score = self.calculate_quality_score(entities, relationships)
        return quality_score
```

## 故障排除

### 1. 游戏数据特有问题

**数据不平衡问题**：
```
Warning: Player segments are highly imbalanced
```
解决：调整玩家分群采样策略或使用加权分析。

**付费数据稀疏**：
```
Warning: Sparse payment data detected
```
解决：增加免费场次分析或使用数据增强技术。

**实时性要求**：
```
Error: Real-time analysis lag detected
```
解决：优化Flink管道或增加计算资源。

### 2. 游戏分析调试

```bash
# 启用游戏分析调试模式
export GAME_ANALYTICS_DEBUG=true
export GRAPHRAG_LOG_LEVEL=DEBUG

# 检查游戏数据质量
python -m game_analytics.data_quality_check --input game_data/

# 验证实体提取效果
python -m game_analytics.entity_validation --output output/
```

## 业务价值实现

### 1. 关键成功指标

- **玩家留存提升**：通过流失预警提升留存率 15-25%
- **收益优化**：通过场次优化提升ARPU 18-30%
- **运营效率**：自动化分析减少人工成本 60-80%
- **决策速度**：实时洞察加速决策速度 50-70%

### 2. 实施建议

1. **第一阶段（1-2周）**：基础数据接入和知识图谱构建
2. **第二阶段（2-4周）**：核心分析功能开发和验证
3. **第三阶段（4-6周）**：生产部署和性能优化
4. **第四阶段（6-8周）**：高级功能和自动化

### 3. 最佳实践

- **数据质量**：确保游戏数据的完整性和一致性
- **实时更新**：建立数据更新管道保持分析时效性
- **业务对齐**：确保分析结果与业务目标对齐
- **持续优化**：基于使用反馈持续改进分析模型

现在您可以开始构建游戏智能分析平台，利用GraphRAG技术实现数据驱动的游戏运营决策！

## 快速开始

1. **生成数据**：`python game_data_generator.py`
2. **构建索引**：`graphrag index --root your_project`
3. **开始查询**：`graphrag query "分析高流失风险玩家特征"`
4. **启动Dashboard**：`streamlit run app/game_analytics_page.py` 